services:
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=events
    ports:
      - "5432:5432"
    volumes:
      - ./.volume/postgres_data:/var/lib/postgresql/data

